# Supabase Configuration
SUPABASE_URL=your_supabase_project_url
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Optional: Disable local filesystem writes explicitly (Vercel Mode)
VERCEL_MODE=true

# Keep default port bindings if testing locally
PORT=7860
